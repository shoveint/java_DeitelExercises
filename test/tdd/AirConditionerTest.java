/*package tdd;

import jdk.internal.org.jline.terminal.Terminal;
import org.junit.jupiter.api.Test;

import java.util.HashMap;

import static jdk.internal.org.jline.terminal.Terminal.MouseTracking.Off;
import static sun.security.util.Debug.args;

//precondition given that i have an Ac
public class AirConditionerTest {
    private Object SetIsOn;

    /**
     * Returns a hash code value for the object. This method is
     * supported for the benefit of hash tables such as those provided by
     * {@link HashMap}.
     * <p>
     * The general contract of {@code hashCode} is:
     * <ul>
     * <li>Whenever it is invoked on the same object more than once during
     *     an execution of a Java application, the {@code hashCode} method
     *     must consistently return the same integer, provided no information
     *     used in {@code equals} comparisons on the object is modified.
     *     This integer need not remain consistent from one execution of an
     *     application to another execution of the same application.
     * <li>If two objects are equal according to the {@link
     *     equals(Object) equals} method, then calling the {@code
     *     hashCode} method on each of the two objects must produce the
     *     same integer result.
     * <li>It is <em>not</em> required that if two objects are unequal
     *     according to the {@link equals(Object) equals} method, then
     *     calling the {@code hashCode} method on each of the two objects
     *     must produce distinct integer results.  However, the programmer
     *     should be aware that producing distinct integer results for
     *     unequal objects may improve the performance of hash tables.
     * </ul>
     *
     * @return a hash code value for this object.
     * @implSpec As far as is reasonably practical, the {@code hashCode} method defined
     * by class {@code Object} returns distinct integers for distinct objects.
     * @see Object#equals(Object)
     * @see System#identityHashCode
     */
  /*  @Override
    public int hashCode() {
        return super.hashCode();
    }

    public static void main(String[] args) {
        
        public void isOn(){
        AirConditionerTest airConditioner = new AirConditionerTest();
        return ;
        }


        //when i increase temperature
        //When i increase Temperature beyond 30
        // Check that Temperature is 3o

        // When i decrease Temperature
        //And when decrease temperature below 16

        @Test
        public void decreaseTemp () {
            AirConditioner airConditioner = new AirConditioner();
            AirConditioner.setTemp(16);
            int decreaseTem = airConditioner.getTemp();
            System.out.println("Temperature increased:");
        }

        // (Action)i turn my AC on
        //  (Assertion) Check That It is ON
        @Test
        public void turnOn () {
            AirConditionerTest airConditioner = new AirConditionerTest();
            airConditioner.turnOn( boolean);
            if (turnOn( boolean) =false);
            boolean on = true;
            System.out.print("boolean, 'on'");
        }

    }
}
*/