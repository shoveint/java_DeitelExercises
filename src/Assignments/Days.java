package Assignments;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Days {
    /*String string = new String();
    String input = "today is sunday, yesterday was Saturday, and Monday comes after sunday";
    StringBuffer buffer = new StringBuffer(input);
    Pattern pattern = Pattern.compile("(^|\\.)\\s*(\\w)");
    Matcher matcher = pattern.matcher(buffer);
    while(matcher.find)){
        buffer.replace(matcher.end() - 1, matcher.end(), matcher.group(2).toUpperCase());
        System.out.println(buffer);
    }

    public Matcher getMatcher() {
        return matcher;
    }

    public void setMatcher(Matcher matcher) {
        this.matcher = matcher;
    }
}
StringBuilder txt = new StringBuilder("today is monday, yesterday was sunday, tuesday comes after today");
int pos = -1;
do{
txt.replace(0, pos + 2, txt.substring(pos + 1, pos + 2).toUpperCase());
pos= txt.indexOf(".", pos) + 1;
}
while(pos > 0 && pos < txt.length());
System.out.println(txt.toString());*/
}